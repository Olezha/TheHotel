Room.availableRooms=SELECT * FROM ROOM WHERE HOTEL_ID IS ?1 AND PERSONS >= ?2 AND ID NOT IN (SELECT ROOM_ID  FROM RESERVATION WHERE REJECTED = FALSE AND (USER_ID IS NOT NULL OR DATEDIFF('MINUTE', CREATED, NOW()) < ?3) AND (CHECK_OUT > ?4 AND CHECK_IN < ?5))
